# 企业小程序销售记账系统 - 项目路线图

> 本文件是项目真实进度源。每次完成开发、修复、文档补齐或重要调研后同步更新。

## 当前阶段

**Phase 5（上线后核心能力补验）** — 域名 ICP 备案、小程序备案、公安联网备案和隐私指引整改均已完成；用户于 2026-08-21 确认小程序已经上线，微信后台当前正式版本号与发布时间仍待只读复核。后端线上为配置版本 `server-038`；三笔 0.10 元真机支付及 `O-20260821-8634` 的普通入口、登录、1 本订单、真实支付、地址填写和结果页均已通过。真实小程序码归因与支付后奖励的两轮生产验收均已通过：第一轮订单 `O-20260822-1803` 生成 1 分销售奖励和 1 分上级直接推荐奖励；第二轮差异化规则生成 3 分销售奖励和 1 分上级直接推荐奖励，均为 `ESTIMATED`、无重复、不打款。管理后台返点规则/记录的空筛选缺陷已修复上线，并补充订单号展示。凭据轮换安全待办已记录，临时测试开关和测试专用规则暂不改动，等待用户后续处理。支付恢复、发货管理、退款客服、正式业务参数及分销合规仍待补验。

## 当前状态快照（2026-08-21）

- [x] 域名 ICP 备案完成（用户于 2026-08-06 确认）
- [x] 微信小程序备案完成（用户于 2026-08-07 确认）
- [x] 自有域名已绑定云托管并可通过 HTTPS 访问
- [x] 线上 `/api/wx/diag` 返回 `wx-login-v4`、`loginMode=real`、`hasAppid=true`、`hasSecret=true`（2026-08-12 实测）
- [x] 线上 `/api/products/storefront` 返回默认直营销售方 `SMFMSVV` 与默认商品；生产商品默认购买数量已由 500 改为 1，接口复核 `defaultQuantity=1`（2026-08-21）
- [x] 云托管新代码部署完成：补齐 `WX_LOGIN_MODE=real`/`WX_PAY_MODE=real` 后从 `feat/ui-redesign`（`eb0cdcd`）重新构建部署成功
- [x] 本地 Phase 5 Task 2 代码完成：显式 `WX_LOGIN_MODE`/`WX_PAY_MODE`、生产启动校验、生产禁用 mock、TLS 校验恢复；已部署上线
- [x] 本地 Phase 5 Task 3 代码完成：买家 JWT、订单归属鉴权、操作日志脱敏、生产管理员安全初始化与 CORS 收敛；线上真实买家登录和基础支付已验证，双买家越权、管理员换密与日志人工验收仍待完成
- [x] 本地 Phase 5 Task 4 代码完成：默认 storefront、无 scene 入口、隐私授权、剪贴板保护、地址/结果页审核信息与首发页面收敛；隐私指引整改和真机基础支付验收已完成
- [x] 历史 0.01 元真机支付冒烟成功（2026-07-23）
- [x] 真实登录、下单与支付确认基础验收：前置版本两笔 0.10 元订单均有 `paidAt`/真实 `wxTransactionId`；当前发布候选 `0.9.0` 另完成一笔 0.10 元普通入口购买闭环（2026-08-21）
- [x] 修复支付成功后的状态确认竞态：小程序仅在后端同时写入 `paidAt`/`wxTransactionId` 后进入地址页，未确认时禁止再次发起支付；服务端拒绝未确认支付订单填写地址，并允许迟到回调/对账补确认且保留 `address_pending`（2026-08-21；`0.9.0` + `server-033` 正常成功路径真机通过，确认超时与重新进入恢复路径待专项验收）
- [x] 小程序 `0.9.0` 体验版从唯一发布候选 `ba200e5` 的隔离构建目录上传成功（2026-08-21；包体 261,339 字节）
- [x] 用户已保存微信公众平台主营类目，平台随后允许进入 `0.9.0` 提审表（2026-08-21；具体保存值待后续只读复核）
- [x] 本地新增合规的买家订单中心页：`pages/buyer/orders/orders` 可不带参数打开并停留，同意隐私指引后自动建立买家会话并分页展示全部订单；显示订单号、状态、金额、数量和下单时间，支持待支付/待填地址继续处理，首页加入“我的订单”入口（2026-08-21；TypeScript 静态检查、定向 JS 编译一致性、JSON 解析和行为测试通过）
- [x] 完成订单中心真机验收并上传 `0.9.1` 体验版：基于 `ba200e5` 的隔离候选通过 `npm ci`、完整 `tsc` 构建、JSON/包内容检查、开发者工具整页打开和用户真机订单列表验收；原上传任务确认后返回 `success`，包体 275,192 字节（2026-08-21）
- [ ] 在提审表填写订单中心 path `pages/buyer/orders/orders`，复核 `0.9.1` 版本描述与其余审核项后再提交审核
- [ ] 将有效支付回调与 `pay-sync` 分开验收，确认 `payment_events` 来源和幂等性
- [ ] 完成实物订单发货管理上线门禁：线上实测 `GET /api/wx-trade/status` 返回 `{"mode":"disabled"}`；自动同步代码存在官方协议字段不匹配，修复并部署前不得将 `WX_TRADE_MODE` 改为 `real`。首发可按微信官方允许的后台手工录入方式兜底，但须先确认账号侧已完成交易结算管理确认并出现“发货信息录入”入口
- [x] 公安联网备案完成（用户于 2026-08-21 确认）

## 已完成

- [x] 项目根目录结构：`src/server`、`src/miniprogram`、`src/admin`、`tests`、`assets`、`docs`
- [x] NestJS 后端初始化（Node.js + NestJS 10 + TypeScript 5）
- [x] TypeORM + MySQL 数据库连接配置
- [x] 13 个核心数据实体（`admin_users`、`sellers`、`products`、`orders`、`order_addresses`、`shipments`、`refund_records`、`payment_events`、`scan_logs`、`reward_rules`、`reward_records`、`operation_logs`、`seller_qrcodes`）
- [x] 管理员认证模块（JWT、登录接口、`JwtStrategy`、`JwtAuthGuard`）
- [x] 操作日志模块（`OperationLogsService`、`OperationLogInterceptor`，best-effort）
- [x] 初始数据库迁移（`InitialSchema1783305843637`），已运行到本地 MySQL
- [x] 全局异常过滤器（`HttpExceptionFilter`）
- [x] 微信小程序原生项目骨架（7 个页面：buyer/index、pay、address、result、group；seller/login、index）
- [x] Vue 3 + Element Plus 管理后台骨架（含 Login.vue）
- [x] 基础 E2E 测试（`test/auth.e2e-spec.ts`），2 个用例通过
- [x] 设计文档：`docs/superpowers/specs/2026-07-05-企业小程序销售记账系统设计.md`
- [x] Phase 1 实施计划：`docs/superpowers/plans/2026-07-05-phase1-infrastructure.md`
- [x] Phase 5 上线整改实施计划：`docs/superpowers/plans/2026-08-06-phase5-miniprogram-launch-readiness.md`
- [x] 本地 MySQL 环境（XAMPP @ `D:\Sql`，数据库 `book_sales`，root 无密码）
- [x] 本地开发存在固定默认管理员播种（生产环境移除固定账号口令播种列入 Phase 5）
- [x] 销售方 CRUD（管理后台）
- [x] 销售方上下级直接推荐关系维护
- [x] 产品配置管理（一期默认一本书）
- [x] `seller_code` 生成规则
- [x] 小程序码记录、下载与解析骨架（当前为占位 SVG；真实可扫码小程序码列入 Phase 5）
- [x] 二维码下载功能
- [x] 二维码生成历史记录
- [x] 扫码访问日志记录
- [x] 二维码公开解析接口（`GET /api/qrcodes/:id/resolve`）
- [x] 产品公开信息接口（`GET /api/products/:id/public`）
- [x] 订单模块（创建、查询、状态流转、地址填写）
- [x] 订单号生成器（`O-YYYYMMDD-XXXX`）
- [x] 内部发货模块（本系统发货记录、售后观察期计算；尚未接入微信订单发货管理）
- [x] 小程序家长端产品页（扫码进入、数量选择、立即购买）
- [x] 小程序地址填写页与结果页
- [x] 小程序入群引导页
- [x] 管理后台订单管理页（列表、详情、改状态、发货）
- [x] 占位微信支付接口（`POST /api/orders/:id/pay`）
- [x] 订单支付状态流转：`pending_payment` → `paid`
- [x] 模拟微信支付单号生成与 `payment_events` 记录
- [x] 小程序支付确认页（`pages/buyer/pay/pay`）
- [x] 小程序 mock 购买流程：扫码 → 产品页 → 创建订单 → 模拟支付 → 填写地址 → 结果页
- [x] 管理后台订单详情显示微信支付单号
- [x] 台账导出模块（`LedgerModule`、`GET /api/ledger/export`）
- [x] 订单明细与销售汇总 Excel 导出
- [x] 订单列表支持按日期范围筛选
- [x] 管理后台“台账导出”页面（筛选、预览、下载）
- [x] 台账导出 E2E 测试
- [x] 售后观察期结算（`aftersale_waiting` → `settlement_ready`）
- [x] 定时结算扫描（`@nestjs/schedule`，每日凌晨 2 点）与手动触发结算
- [x] 返点规则管理（CRUD、`GET /api/rewards/rules`）
- [x] 返点规则匹配：产品+销售方 → 产品 → 销售方 → 默认规则
- [x] 返点记录生成（`reward_records`）
- [x] 一层直接推荐奖励（基于 `sellers.parentId`）
- [x] 返点记录管理（列表、详情、状态更新）
- [x] 管理后台“返点规则”和“返点记录”页面
- [x] 售后观察期与返点相关 E2E 测试
- [x] GitHub 仓库：`https://github.com/Imsage323/book-sales-referral`
- [x] 后端 `Dockerfile` 与 `.dockerignore`
- [x] 容器启动自动运行 TypeORM 迁移
- [x] 微信云托管后端部署（dev 环境）
- [x] 云托管 MySQL 数据库与 `book_sales` 数据库创建
- [x] 基础生产环境变量配置（`NODE_ENV=production`、数据库、JWT、微信登录/支付变量；Phase 5 新增 mode 变量与证书仍待部署前脱敏复核）
- [x] 后端启用 CORS 支持管理后台跨域访问
- [x] 管理后台生产环境 API 地址配置（`.env.production`）
- [x] 管理后台静态构建产物（`src/admin/dist`）
- [x] 管理后台集成到后端服务（`@nestjs/serve-static` 静态文件服务 + 仓库根目录 `Dockerfile` 多阶段构建）
- [x] 微信支付真实集成代码（mock/真实双模式；生产 fail-closed 代码已完成，云托管部署与发布候选完整验收仍待办）
  - `wx.config.ts` 集中读取 `WX_*` 环境变量，`main.ts` 开启 `rawBody`
  - `payments` 模块：JSAPI 下单、`wx.requestPayment` 参数二次签名、回调验签 + AES-256-GCM 解密、按商户订单号主动查询（官方 `wechatpay-axios-plugin`，微信支付公钥模式）
  - `POST /api/wx/login`（code 换 openid；占位 openid 仅允许 development/test 显式 mock）与 `POST /api/wx/notify`（验签失败 401 + FAIL，成功幂等落库）
  - 订单侧：`payOrder` 真实模式返回支付参数不置 paid、`POST :id/pay-sync` 主动对账、`markPaid` 回调/对账/mock 共用
  - 小程序端：启动静默登录并只缓存买家 JWT；下单、扫码日志、订单详情、地址、支付和对账均从认证上下文取得 openid；支付页接入 `wx.requestPayment` + 对账

## 历史验证快照

> 下列结果来自对应开发阶段，不能替代 Phase 5 最终发布候选的全量验证。

- `cd src/server && npm run build` ✅ 通过
- `cd src/server && npm run test` ✅ 通过（24 个单元测试，含 wx-pay 签名/验签解密/金额校验与 orders 真实模式用例）
- `cd src/server && npm run test:e2e` ✅ 30 个测试通过（auth + sellers + products + qrcodes + scan-logs + orders + shipments + ledger + settlements + reward-rules + reward-records + payments）
- `cd src/miniprogram && npm run build` ✅ 通过
- `cd src/admin && npm run build` ✅ 通过
- 本地验证后端 `/api/auth/login` ✅ 通过
- 本地验证 `/login`、`/sellers` 等前端路由返回 `index.html` ✅ 通过
- 本地验证 `/assets/...` 静态资源 ✅ 通过
- MySQL 数据库表确认 ✅ 13 张业务表 + `migrations` 表
- 2026-08-06 线上 `/api/wx/diag` ✅ 返回 `wx-login-v3`、`hasAppid=true`、`hasSecret=true`（仅证明版本和配置存在，不代表真实登录闭环已验收）
- 2026-07-23 真机 0.01 元 `wx.requestPayment` ✅ 冒烟成功（当前发布候选的有效回调落库仍待验收）
- 2026-08-21 小程序 `0.9.0` + 后端 `server-033` 真机核心购买闭环 ✅ 订单 `O-20260821-8634` 完成 1 本/0.10 元真实支付、地址提交和结果页展示；地址内容完整换行，无上下裁切

## Phase 5 验证（2026-08-06）

- `cd src/server && npm run build` ✅ 通过
- `cd src/server && npm run test -- --runInBand` ✅ 34 个单元测试通过
- `cd src/server && npm run test:e2e -- --runInBand` ✅ 32 个 E2E 通过
- 生产安全用例 ✅ mock 登录/支付在 production 返回 503，订单保持 `pending_payment`
- TLS 用例 ✅ 微信登录请求不再设置 `rejectUnauthorized: false`

## Phase 5 最近验证（2026-08-07）

- `npm.cmd --prefix .\src\server run build` ✅ 通过
- `npm.cmd --prefix .\src\server run test -- --runInBand` ✅ 39 个单元测试通过
- `npm.cmd --prefix .\src\server run test:e2e -- --runInBand` ✅ 35 个 E2E 通过
- `npm.cmd --prefix .\src\miniprogram run build` ✅ 通过
- 买家订单安全用例 ✅ 买家只能访问自己的订单，买家/管理员 token 不可混用，无 token 拒绝访问
- 日志安全用例 ✅ 登录及买家接口不保存请求 body，管理接口敏感字段递归脱敏
- Task 4 storefront 用例 ✅ 未配置默认销售方/商品时返回 503，显式配置后才返回直营入口
- Task 4 小程序配置 ✅ `app.json` 启用隐私检查，首发仅保留家长购买页面，项目与本机配置均启用域名校验
- Task 4 构建后复验 ✅ 39 个单元测试、37 个 E2E、server/miniprogram build、JSON 解析和 `git diff --check` 通过

## 环境配置

`src/server/.env` 已配置（本地开发）：

```env
NODE_ENV=development
PORT=3000
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=
DB_NAME=book_sales
JWT_SECRET=<local-development-secret>
JWT_EXPIRES_IN=7d
DEFAULT_PRODUCT_ID=595f08d3-d92b-4c83-9445-1d743633ad1c
DEFAULT_SELLER_CODE=SMFMSVV
```

Phase 5 Task 2 未修改真实 `src/server/.env`；2026-08-07 经用户确认补入本地直营销售方与默认商品参数。本地若需 mock，必须在 `NODE_ENV=development/test` 下显式设置 `WX_LOGIN_MODE=mock`、`WX_PAY_MODE=mock`。2026-08-12 云托管已补入 `WX_LOGIN_MODE=real`、`WX_PAY_MODE=real` 并完成新版部署，storefront 变量已经公开接口验证生效。

## 进行中

- [ ] Phase 5 上线整改与发布准备：Task 2、Task 3、Task 4 代码与自动验证完成，云托管部署、微信后台配置和 `0.9.0` 普通入口核心购买闭环真机验收已完成；其余发布门禁待继续处理，详见 `docs/superpowers/plans/2026-08-06-phase5-miniprogram-launch-readiness.md`

## 阶段计划

**Phase 5：微信小程序上线整改与发布准备** ⏳ 进行中

实施任务书：`docs/superpowers/plans/2026-08-06-phase5-miniprogram-launch-readiness.md`

### Phase 5.1：发布阻塞设计确认

- [ ] 确认正式售价、售后观察期、快递方案、客服渠道、入群二维码和首发范围
- [ ] 确认“一层直接推荐奖励”的合规方案；结论明确前正式环境保持关闭（代码已改为 `REFERRAL_REWARD_ENABLED` 显式开启、`REFERRAL_REWARD_CENTS_PER_BOOK` 配置正整数分/本，缺省不生成推荐奖励；技术验收可另用 `REWARD_ESTIMATE_ON_PAID_ENABLED` + 指定 `REWARD_ESTIMATE_SELLER_ID`，仅为该测试下线在真实付款确认后生成 `ESTIMATED` 预估记录，不改变订单状态或自动打款）
- [ ] 确认退款、微信发货管理和返点冲销的数据流及迁移范围

### Phase 5.2：核心能力与安全整改

- [x] 生产环境登录/支付配置 fail-closed，禁止占位 openid 和 mock 已支付（代码与自动测试完成；2026-08-12 云托管已补 `WX_LOGIN_MODE=real`/`WX_PAY_MODE=real` 并完成部署，diag 实测 `loginMode=real`）
- [x] 恢复微信接口 TLS 证书校验（代码与自动测试完成；需关闭自签代理并完成真实登录人工验收）
- [x] 买家会话、订单归属鉴权、敏感日志脱敏、生产管理员安全初始化和 CORS 收敛（代码与自动验证完成；线上管理员换密、真机双账号和数据库日志人工验收待办）
- [x] 无 `scene` 默认商品入口、隐私授权、客服与审核体验（代码与自动验证完成；默认 storefront 环境变量、`request` 合法域名、《小程序用户隐私保护指引》已完成配置/验证；`0.9.0` 普通入口、地址和结果页真机展示通过，客服按钮实际联络能力待验收）
- [x] 接入真实微信小程序码并完成真机归因验证（2026-08-22；测试码进入并生成订单 `O-20260822-1803`，生产订单归属测试下线 `SF7D4NP`，1 本/0.10 元真实支付确认）
- [x] 修复管理后台返点规则/记录空筛选查询并补充订单号展示（提交 `8a3e0f6`；前端不再发送空枚举参数，后端兼容空可选筛选，加载失败明确提示；admin build、server build、72/72 单元测试、46/46 E2E 通过，`server-037` 上线后后台无筛选可正常显示 `O-20260822-1803` 两条记录）
- [ ] 接入微信订单发货管理：本地模块、同步状态、管理后台重试和诊断接口已实现，但 2026-08-21 按微信当前官方接口逐字段复核发现 `order_number_type` 与订单字段组合、`upload_time` 格式不符；线上 `WX_TRADE_MODE=disabled`，尚未发生错误上传。需修复并自动验证后再部署、启用和完成真实发货；在此之前首发只能使用微信后台手工录入发货信息
- [ ] 实现微信退款、退款状态同步和返点冲销最低闭环
- [ ] 移除生产占位售价、奖励和业务参数
- [ ] 以脱敏方式复核当前发布候选全部微信支付配置、通知地址和证书有效性

### Phase 5.3：发布候选与全量验证

- [x] 固化并推送唯一发布 commit `ba200e5`；小程序 `0.9.0` 体验版与云托管后端 `server-033` 均来自该提交的隔离构建目录
- [x] 当前发布候选通过 server build、63/63 单元测试、44/44 E2E、admin build、miniprogram build（2026-08-21；E2E 使用工作区隔离 MariaDB 3307，未连接原本地库或生产库）
- [x] 当前发布候选 `0.9.0` 真机完成普通入口 → 登录 → 创建 1 本/0.10 元订单 → `wx.requestPayment` → 后端支付确认 → 地址 → 结果页；订单 `O-20260821-8634` 的金额、数量、地址状态和完整换行展示均正确（2026-08-21）
- [ ] 真机完成普通入口/销售码 → 登录 → 支付 → 回调/对账 → 地址 → 发货 → 售后 → 退款闭环

### Phase 5.4：提审与发布

- [x] 小程序备案最终通过（用户于 2026-08-07 确认）
- [x] 上传小程序 `0.9.0` 体验版（2026-08-21；发布候选 `ba200e5`，包体 261,339 字节）
- [x] 上传小程序 `0.9.1` 体验版（2026-08-21；基于 `ba200e5` 的隔离候选，仅叠加订单中心和首页入口，包体 275,192 字节；订单列表真机验证通过）
- [ ] 完成其余提交审核门禁后提交微信审核（`0.9.0` 普通入口核心购买闭环已通过真机回归；`0.9.1` 订单中心已真机通过并上传，待填写 path 和最终复核）
- [ ] 审核通过后，经用户确认正式发布
- [ ] 发布后重新生成 release 销售码，完成生产冒烟和首单观察

### 已完成阶段

**Phase 4：部署准备（GitHub 仓库 + 微信云托管容器部署）** ✅ 已完成

- [x] 创建 GitHub 仓库 `Imsage323/book-sales-referral` 并推送本地代码
- [x] 为后端添加 `Dockerfile` 与 `.dockerignore`
- [x] 添加 TypeORM 迁移脚本（`migration:run` / `migration:show`）
- [x] 在微信云托管绑定仓库并配置环境变量
- [x] 在微信云托管创建/连接 MySQL 数据库并运行首次迁移
- [x] 验证云托管后端服务可访问（健康检查或登录接口）

**Phase 3.2（真实化）：微信支付真实集成** ✅ 代码完成（生产安全与当前发布候选验收转入 Phase 5）

- [x] 微信小程序登录接口代码（`POST /api/wx/login`；配置齐全时调用 `jscode2session`，未配置时仍会返回占位 openid，Phase 5 必须移除生产保底并重验真实登录）
- [x] 微信支付统一下单接口（接入商户号 / APIv3，JSAPI 下单 + 支付参数二次签名）
- [x] 微信支付回调处理（验签、幂等、金额校验、更新订单状态）
- [x] 小程序端调用 `wx.requestPayment` 调起微信支付（含取消重试与 `pay-sync` 主动对账）
- [x] 替换模拟支付单号生成逻辑，写入真实 `wxTransactionId`（mock 模式保留供本地开发与测试）
- [x] 历史微信支付配置曾支持 2026-07-23 真机支付（当前发布候选的全部支付变量、通知地址和证书仍待脱敏复核）
- [x] 支付代码已推送到 GitHub `master`（`d18c2f9`），登录加固版本已部署；2026-08-06 线上诊断为 `wx-login-v3`
- [x] 历史真机 0.01 元 `wx.requestPayment` 冒烟成功（2026-07-23）
- [x] 当前发布候选基础闭环：静默登录 openid → JSAPI 下单 → `wx.requestPayment` → 后端确认支付并记录真实 `wxTransactionId` → 地址/结果页（2026-08-21 两笔 0.10 元订单实测）
- [ ] 有效支付回调：`POST /api/wx/notify` 验签、解密、金额校验与 `payment_events` 落库；`pay-sync` 仅作兜底

**Phase 3.3：售后观察期与返点** ✅ 已完成

- [x] 售后观察期自动/手动结算
- [x] 返点规则配置（固定金额/比例/阶梯）
- [x] 预计返点与最终返点计算
- [x] 一层直接推荐奖励
- [x] 返点溯源记录
- [x] 管理后台返点处理页

**Phase 3.4A：基础订单/发货台账与销售汇总** ✅ 已完成

- [x] 订单明细与销售汇总 Excel 导出

**Phase 3.4B（扩展）：返点及更多台账维度**

- [ ] 返点台账导出（依赖 Phase 3.3 的 reward_records 数据）
- [ ] 更多汇总维度：按产品、按地区、按时间段
- [ ] 发货明细独立台账 sheet

## 阻塞

- [业务决策] “一层直接推荐奖励”存在多级分销合规风险；结论明确前正式环境不得启用
- [业务参数] 正式售价、售后观察期、快递方案、客服与退款 SOP、入群二维码尚未确认
- [平台配置] `request` 合法域名已完成配置；《小程序用户隐私保护指引》剪贴板用途驳回项已整改解决（用户于 2026-08-21 确认）
- [提审配置] 用户已保存主营类目并进入提审表；版本描述、无需测试账号、非企业微信专用、常规审核和采集用户隐私已填写/确认，图片与视频按选填留空。`0.9.1` 已包含并真机验证订单中心，当前可如实填写 path `pages/buyer/orders/orders`；尚未填写、未提交审核
- [核心能力] 真实小程序码已部署并生成首个测试下线码，真机归因待验；微信退款及返点冲销尚未实现。“购物订单”未开通已证实不是当前 JSAPI 支付的持续阻断项，但实物电商的订单发货管理仍是上线履约门禁。线上 `WX_TRADE_MODE=disabled`，自动同步代码存在已确认的官方协议不匹配；修复前可按官方说明在微信后台手工录入每笔发货信息，账号侧入口与交易结算管理确认状态待登录公众平台核对
- [生产安全] fail-closed、TLS、订单归属鉴权、日志脱敏和默认管理员整改代码已完成且已部署上线（2026-08-12）；线上管理员换密确认和真机人工验收尚未完成
- [支付状态一致性] 代码已修复原竞态：客户端保存“已提交微信支付、待后台确认”状态并重试 `pay-sync`，确认前不进入地址页且不再次下单支付；服务端拒绝 `pending_payment` 填地址，迟到回调/对账可补齐支付字段并保留 `address_pending`。server build、63/63 单元测试、44/44 全量 E2E、admin build、miniprogram build 已通过；`0.9.0` + `server-033` 正常成功路径真机已通过，支付确认超时与重新进入恢复路径仍待专项验收
- [迁移可复现性] 已修复并在全新隔离空库验证：历史扩容迁移在 `seller_qrcodes` 尚不存在时安全跳过，`InitialSchema` 直接创建 `imageUrl varchar(2000)`，CLI 仅加载数字时间戳开头的正式迁移。`migration:run` 首次执行 3/3 成功、重复执行无待执行迁移，字段实测为 `varchar(2000)`；没有连接或修改生产数据库
- [发布验收] `0.9.0` 的一笔真实支付、地址提交与结果页完整闭环已通过；`0.9.1` 在同基线上新增的订单中心已真机通过并上传。有效回调与 `pay-sync` 来源分离、销售码归因、发货/售后/退款完整闭环尚未通过
- [发货合规] 微信官方当前文档明确：实物电商需接入订单发货管理，未完成 API 对接时可以在小程序后台手工录入。当前自动同步若直接启用会因 `order_number_type=2` 却传 `mchid/out_trade_no`、以及 `upload_time` 不是 RFC 3339 而失败；修复前禁止启用 `real`，首发若继续推进必须采用逐单人工录入并完成账号侧确认

## 待确认

- [ ] 正式售价（生产库商品价格仍为 0.10 元；基础支付验收已于 2026-08-21 完成，下一步须经用户确认改回 168.00 元，代码默认占位价 0.01 元仍需核对）
- [ ] 正式售后观察期、发货时限、快递公司及物流编码
- [ ] 客服渠道、退款处理人与人工兜底 SOP
- [ ] 入群二维码维护方式，以及支付后入群入口是否保留
- [ ] 正式销售奖励参数及“一层直接推荐奖励”合规方案
- [ ] 首发是否包含尚未完整实现的销售方/老板端
- [ ] 抖店渠道：资质审核与铺货进行中（用户侧）；抖店订单是否纳入本系统台账/返点体系待评估
- [x] 直营销售方 `DEFAULT_SELLER_CODE=SMFMSVV` 与默认在售商品 `DEFAULT_PRODUCT_ID=595f08d3-d92b-4c83-9445-1d743633ad1c` 已由用户确认并写入本地与云托管环境；2026-08-12 经 `/api/products/storefront` 公开接口验证已生效

## 资质与外部状态

- [x] 书名、是否允许多本购买：书名《高三学业生涯导航日历》，允许多本购买
- [x] 出版物经营许可证：已取得
- [x] 微信认证：已完成（小程序信息、类目、认证均已通过，2026-07-21 截图确认）
- [x] 微信支付商户号与密钥：当前配置已支持 2026-08-21 两笔 0.10 元真机支付及后端支付确认；完整配置、通知地址和证书有效期仍待脱敏复核
- [x] 域名 ICP 备案：已完成（用户于 2026-08-06 确认）
- [x] 自有域名与 HTTPS：`优学启航.com`（punycode `xn--4oqx7jk6ghq7b.com`）已绑定云托管，主域 + www 外网验证 200；TrustAsia 免费 DV 证书 2026-11-01 到期需续期
- [x] 微信小程序备案：已完成（用户于 2026-08-07 确认）
- [x] 公安联网备案：已完成（用户于 2026-08-21 确认）

## 技术栈

- 后端：Node.js + NestJS 10 + TypeScript 5 + TypeORM + MySQL 8
- 小程序：原生微信小程序
- 管理后台：Vue 3 + Element Plus + TypeScript
- 本地 MySQL：XAMPP（`D:\Sql`）

## 重要修复记录

- 修复 `Order` 和 `Seller` 实体中 `@Index({ unique: true })` 与 `@Column({ unique: true })` 重复导致的迁移失败
- 修复 `OperationLogInterceptor` 在测试关闭阶段的数据库连接错误，改为 best-effort 捕获
- 修复 E2E 测试中 `supertest` 的导入方式和全局路由前缀问题
- 关闭 TypeORM `synchronize`，避免与现有迁移脚本冲突导致启动时报错
- 修复管理后台 `vue-tsc` 与 TypeScript 5.9 不兼容问题，将 `typescript` 锁定为 `~5.3.3`

## 最近更新

- 2026-08-22：进度对齐与买家页服务方展示统一——用户确认：购物订单审批**仍未通过**，但真机 0.1 元支付+回调落库已跑通，此前"付款能力限制=未接入购物订单"的归因不准确；商品价格维持 0.10 元验收价（用户决定，改回 168 元仍待办）；`WX_TRADE_MODE` 保持 disabled，发货→微信 `upload_shipping_info` 真实闭环尚未验证（验证方法：后台对一笔已支付订单发货，看 `wxSyncStatus`）。买家页服务方展示采用统一官方品牌方案：`pages/buyer/index/index.wxml` 的 source-line 由动态「企业直营/销售专属 + seller.name」改为静态「官方直营 由耘梦优学启航提供服务」，销售方名称不再外显（避免测试/随意命名漏给买家）；`seller_code` 归因逻辑不变，仅用于返点计算。tsc build 通过；销售方「对外显示名」字段作为可选增强待议
- 2026-08-22：管理后台筛选修复与第二轮差异化返点验收完成——提交 `8a3e0f6` 仅包含 9 个返点前后端/测试文件并已推送，admin build、server build、72/72 单元测试、46/46 E2E 通过；自动部署 `server-037` normal、流量 100%，线上返点规则无筛选显示 2 条，返点记录无筛选显示订单 `O-20260822-1803` 的 2 条预估记录且订单号已可见。生产新增仅绑定产品《高三学业生涯导航日历》与测试下线“返点验收下线-20260821”的每本 3 分规则，保留默认每本 1 分规则；配置版本 `server-038` 已开启仅限该测试下线的付款后预估计算与上级每本 1 分推荐奖励，状态 normal、流量 100%。用户已完成新订单付款验收，确认同一新订单恰有两条记录：下线销售奖励 3 分、企业直营推荐奖励 1 分，均为 `ESTIMATED`、无重复。临时开关、测试专用规则和凭据轮换均暂不改动，后续由用户处理
- 2026-08-22：真实销售码与支付后奖励第一轮生产验收通过——用户通过测试下线码完成订单 `O-20260822-1803`，后台核对为 1 本/10 分、真实支付字段完整、销售方 ID 对应 `SF7D4NP`；生成两条且仅两条 `ESTIMATED` 记录：测试下线销售奖励 1 分、企业直营上级推荐奖励 1 分，公式均为“1分/本 × 数量1 = 1分”。随后将 `REFERRAL_REWARD_ENABLED` 与 `REWARD_ESTIMATE_ON_PAID_ENABLED` 恢复为 `false`，配置版本 `server-036` normal、流量 100%，已有验收记录保留，后续订单不再生成测试奖励。另定位管理后台返点列表显示 `No Data` 的原因：前端把未选择的 `rewardType`/`status` 作为空字符串发送，后端正确返回 400，但页面未展示错误；直接接口按订单查询可正常返回两条记录
- 2026-08-21：提交 `b85e007` 已推送并由 GitHub 自动部署为 `server-034`，状态 normal、流量 100%；生产启动迁移将 `seller_qrcodes.imageUrl` 扩为 `longtext`，随后成功创建测试下线“返点验收下线-20260821”（销售码 `SF7D4NP`，上级为企业直营）及真实 JPEG 小程序码，公开解析接口返回的销售方、商品、1 本/0.10 元与 `scene` 均正确。按用户确认的“先验收付款后奖励、不打款”口径，生产配置仅对该测试下线开启每本 1 分销售奖励及上级 1 分直接推荐奖励，配置版本 `server-035` 已 normal、流量 100%；下一步为真机扫码付款后核对两条 `ESTIMATED` 记录，验收完成后立即关闭两个启用开关。CloudBase 服务详情查询在工具日志中返回了现有环境变量原文，虽未提交代码或公开到网站，仍须在测试完成后轮换数据库密码、JWT、微信 AppSecret、微信支付私钥和 APIv3 Key
- 2026-08-21：完成真实销售码与推荐奖励安全开关的本地开发——生产模式通过微信 `stable_token` 获取接口凭证并调用 `getUnlimited`，固定打开已发布的 `pages/buyer/index/index`，使用去连字符的 32 位二维码记录 ID 作为 `scene`；微信返回 JSON 错误时明确失败，不写入空记录。二维码图片兼容 PNG/JPEG/SVG，`seller_qrcodes.imageUrl` 从 `varchar(2000)` 扩为 `longtext`，新增升级迁移且全新隔离库正式 `migration:run` 4/4 成功。一层推荐奖励移除硬编码 `0.5 分/本`，改为环境变量显式开启和正整数分/本配置，默认关闭。按用户调整后的验收口径，新增支付后预估模式：必须同时开启 `REWARD_ESTIMATE_ON_PAID_ENABLED` 并绑定唯一 `REWARD_ESTIMATE_SELLER_ID`，真实付款确认后只为该测试下线生成 `ESTIMATED` 销售奖励和上级推荐奖励；重复回调/对账由订单行事务锁保证幂等，订单仍保持可填写地址的状态；后续售后结算会把原预估记录升级为 `READY`，不重复新增。隔离 E2E 使用 1 本、1 分/本验证“付款后两条预估记录 → 地址/发货 → 售后转为可结算”完整状态变化。server 71/71 单元测试、44/44 E2E、server/admin build 均通过；未修改生产数据库、环境变量或线上版本。原本地 MariaDB 3306 仍因 InnoDB 系统表空间异常无法启动，本轮改用 `%TEMP%` 下独立 3307 实例，未修复或覆盖原数据
- 2026-08-21：用户确认小程序已经上线；该信息来自用户确认，微信后台当前正式版本号、发布时间和审核记录尚未在本轮只读复核
- 2026-08-21：用户确认 `auto_preview` 真机订单列表显示正常；随后对隔离候选再次核对 AppID、订单中心注册和 source map，门禁通过后发起 `0.9.1` 上传，版本描述为“新增买家订单中心，支持查看全部购买订单及继续处理；首页新增我的订单入口；保留默认1本、支付确认和长地址显示修复。”用户在开发者工具确认后，原任务 `confirmation_upload_d3a8fd45-6d3b-4f3f-9d47-2f9bd79f7f1a` 查询结果为 `success`，上传包体 275,192 字节。当前 `0.9.1` 体验版已上传；未填写提审 path，未提交审核，未正式发布
- 2026-08-21：完成订单中心上传前隔离构建与预览推送——工作区 HEAD 仍为已验收基线 `ba200e5`，因当前小程序目录另有未提交的销售端/多端改动，本轮从该提交导出 52 个基线文件，仅叠加订单中心及首页入口相关文件，形成 57 个源码文件的隔离候选；AppID 复核为 `wx9ce9139ed55e9d51`，`app.json` 只注册 6 个买家端页面。隔离候选 `npm ci` 与完整 `tsc` 构建通过，相关运行文件哈希与工作区一致，无 source map、无敏感命名文件，开发者工具成功整页打开 `pages/buyer/orders/orders` 且 console 无记录；单文件编译接口返回工具内部错误 `p is not defined`，截图/自动化读取通道无返回，未据此冒充视觉验收。`auto_preview` 已成功将 273,524 字节预览包推送到开发者微信；真机订单列表仍待用户确认，尚未调用 `upload`，未填写提审 path，未提交审核
- 2026-08-21：本地完成买家订单中心页——新增 `pages/buyer/orders/orders`，主 path 无需参数即可打开并停留；页面先遵守既有隐私授权边界，同意后才建立买家会话，随后复用现有 `GET /api/buyer/orders` 按时间倒序分页展示当前买家的全部交易订单，包含订单号、状态、实付金额、数量和下单时间；待支付订单可继续支付，已支付订单可继续填写地址，已提交地址后的订单可查看详情。商品首页新增“我的订单”入口并在 `app.json` 注册页面。未改后端、数据库、价格、环境变量或线上配置；TypeScript `--noEmit`、定向生成 `.js` 与源码一致性、JSON 解析和 Node 行为测试通过。尚未在微信开发者工具/真机验证，未上传新体验版，未填写提审 path，未提交审核
- 2026-08-21：用户保存主营类目后成功进入 `0.9.0` 提审表；已填写 81/200 字版本描述（实体图书浏览、数量选择、微信支付、收货地址、默认 1 本、支付防重复、长地址显示修复），确认“无需登录账号即可使用”“仅在企业微信中运行：否”“常规审核”“采集用户隐私”，图片与视频按选填留空，未点击“提交审核”。表单明确提醒交易类小程序未提交订单中心 path 将无法通过审核；其官方说明要求主 path 不带参数即可打开统一页面并展示全部资金交易订单。代码复核确认 `0.9.0` 没有订单列表页：`pages/buyer/index/index` 仅为商品购买首页，`pages/buyer/result/result` 必须带 `orderId`，无参数会提示“缺少订单信息”。因此订单中心 path 暂时保持空白，需先开发合规订单列表页、验证并重新上传版本，不能填写一个审核时打不开或不展示全部订单的假路径
- 2026-08-21：完成微信公众平台提审入口只读核对——账号已认证、小程序已备案、隐私指引显示已更新、UGC 已声明，`0.9.0` 为当前体验版且无审核版/线上版；关联微信支付商户正常，交易保障分 100、近 30 日投诉为 0，“购物订单”未开通但不是本次提审直接阻塞。点击“提交审核”后平台明确提示“请设置小程序主营类目后再提交审核”；进入设置页确认两个服务类目均已通过，可选主营类目为“文娱 > 出版物”与“商家自营 > 图书报刊/音像/影视/游戏/动漫”，当前年度剩余 5 次修改机会。推荐选择与实际实体图书自营业务一致的后者；因保存会消耗 1 次修改额度，本轮停在确认弹窗，未保存、未提交审核、未发布。通知中心 2026-08-20 的实物交易类提醒仅限制广告/其他小程序/半屏等跳入跳出能力，当前首版不依赖这些入口，不是普通支付或本次提审的直接原因
- 2026-08-21：完成提审前订单发货管理只读复核——微信支付官方当前开发指引明确，交易类小程序应按运营规范接入订单发货管理，否则可能被限制正式环境支付；微信开放文档进一步说明实物电商分批接入，未完成 API 开发时允许在公众平台手工录入。线上经管理后台鉴权调用 `GET /api/wx-trade/status` 返回 200、`{"mode":"disabled"}`，因此尚未触发错误上传。逐字段对比官方 `uploadShippingInfo` 文档发现两项确定缺陷：代码以 `order_number_type=2`（微信支付单号）发送 `mchid/out_trade_no`，官方要求该组合使用类型 1，类型 2 必须传 `transaction_id`；代码生成 `yyyy-MM-dd HH:mm:ss`，官方要求 RFC 3339（示例 `2022-12-15T13:29:35.120+08:00`）。另有自定义消息跳转接口仍使用旧的 `update_order_detail_path`，当前发货管理接口为 `set_msg_jump_path`，但自定义跳转并非首发必需，可暂用平台默认页。结论：不得直接把 `WX_TRADE_MODE` 改为 `real`；最快安全路径是先确认公众平台已完成交易结算管理并可手工录入发货，`0.9.0` 可继续准备提审，自动同步另行修复、部署和真单验证
- 2026-08-21：小程序 `0.9.0` 体验版核心购买闭环真机通过——用户在普通入口完成登录、创建 1 本/0.10 元订单、真实支付、填写地址并进入结果页；订单号 `O-20260821-8634`，结果页显示“地址已提交”，金额与数量正确，长地址完整换行且无上下裁切。该证据确认当前发布候选的正常成功路径和地址显示修复有效；未记录用户姓名、手机号和详细地址。支付确认超时/重新进入恢复、有效回调与 `pay-sync` 来源分离仍待专项验收
- 2026-08-21：发布候选 `ba200e5` 已推送并完成同提交后端部署——先确认微信云托管目标为 `dev-d4gla11po1a7b9914 / server`，保留原 22 项环境变量、3000 端口、`PUBLIC + MINIAPP` 访问方式和数据库连接配置；从提交重新导出的部署目录共有 220 个受控文件，逐一哈希一致且无真实 `.env`。push 触发仓库自动部署 `server-032`，随后隔离目录部署又生成内容相同的 `server-033`；未删除版本，最终 `server-033` 状态 normal、流量 100%，`server-032`/`server-031` 保留为无流量回滚版本。自有域名 `/api/wx/diag` 返回 200、`wx-login-v4`、`loginMode=real`，`/api/products/storefront` 返回 200、价格 10 分、`defaultQuantity=1`，证明新容器启动、数据库连接和公开基础接口正常。尚未提交微信审核或公开发布小程序；体验版普通入口核心购买闭环已于同日后续通过真机回归
- 2026-08-21：上传小程序 `0.9.0` 体验版——先将支付状态确认、地址显示、空库迁移及对应测试固化为唯一候选提交 `ba200e5`，再从该提交导出隔离目录、按 lockfile 全新安装依赖并编译；9 个运行时 `.js` 与提交逐一哈希一致，AppID 为 `wx9ce9139ed55e9d51`，仅注册 5 个家长端页面且无运行时 source map。微信开发者工具登录和 Orca 授权成功，`upload` 返回成功，包体 261,339 字节；随后后端同提交部署完成。尚未提交微信审核或公开发布；体验版普通入口核心购买闭环已于同日后续通过真机回归
- 2026-08-21：修复收货地址页输入文字在 Android 真机被上下裁切——根因是原生 `<input>` 只有垂直 `padding`、没有显式高度，内容区在部分真机上不足；统一设置输入框 `height/line-height: 88rpx`，垂直内边距改为 0，保留 20rpx 水平内边距。miniprogram build 与 `git diff --check` 通过；已随 `0.9.0` 上传体验版，真机已确认实际地址内容可完整换行显示且无上下裁切
- 2026-08-21：修复全新空库迁移顺序问题——`IncreaseQrcodeImageUrlLength1752080000000` 增加 `hasTable('seller_qrcodes')` 守卫，空库中先安全记录该历史迁移；`InitialSchema1783305843637` 将二维码地址字段直接创建为 `varchar(2000)`，已有库仍保留扩容逻辑；`data-source.ts` 只加载数字时间戳开头的迁移，防止测试文件被 CLI 误执行。全新隔离数据库直接运行正式 `migration:run` 3/3 成功，重复执行显示无待执行迁移，字段实测为 `varchar(2000)`；server build、63/63 单元测试、44/44 全量 E2E 通过，未连接生产库
- 2026-08-21：完成支付状态一致性修复与隔离库复验——小程序不再把 `wx.requestPayment` 的 success 直接当作业务支付完成，而是最多 4 次调用 `pay-sync`，仅在后端订单同时存在 `paidAt` 与真实 `wxTransactionId` 后进入地址页；确认超时会持久化“已提交支付”并提示不得再次支付，返回页面后只能重新确认。服务端禁止 `pending_payment` 订单填写地址，支持迟到回调/主动对账确认 `paid`/`address_pending`，并对已确认订单顺序重复通知幂等返回。因原 `D:\Sql\mysql\data` 的 MariaDB 自身崩溃，本轮在工作区 `.tmp/e2e-mariadb-20260821` 建立独立 3307 测试实例；订单/支付定向 E2E 14/14、全量 E2E 44/44、server build、63/63 单元测试、admin build、miniprogram build 均通过，未连接生产。空库迁移顺序问题随后已修复并通过正式 `migration:run` 复验；小程序 `0.9.0` 与后端 `server-033` 已部署，正常成功路径真机已通过，支付确认超时与重新进入恢复路径待专项验收
- 2026-08-21：修正小程序初始购买数量——线上唯一在售商品《高三学业生涯导航日历》的 `defaultQuantity` 从 500 改为 1；管理接口更新返回 200，随后公开 `/api/products/storefront` 复核价格仍为 10 分、商品仍在售且 `defaultQuantity=1`。小程序首页按该字段初始化数量，因此无需改代码或重新部署，完全退出后重新进入即可生效
- 2026-08-21：重新从零核验支付失败归因——线上先出现一次公开 API 503、随后恢复 200，因与历史失败没有同一时间/订单证据，仅保留为独立可用性线索，不认定为根因。真机连续完成两笔 0.10 元支付：`O-20260821-6261` 后台为 `paid`，`O-20260821-2266` 后台为 `address_pending` 且地址已保存；两笔均为数量 1、金额 10 分，并写入 `paidAt` 和真实 `wxTransactionId`。结论：当前 JSAPI 下单、`wx.requestPayment`、微信扣款与后端支付确认可用；“未开通购物订单导致支付失败”的旧归因证据不足，已撤回，购物订单/发货管理应作为独立平台能力继续核验；腾讯云托管网页/域名也没有被本次证据指向为支付根因。现有数据无法区分支付由有效回调还是 `pay-sync` 落库，仍需分开验收。用户同时确认隐私指引驳回项和公安联网备案均已解决
- 2026-08-20：购物订单开通路径调研收官——「类目自助开通」路线验证失败：小程序已新增服务类目「商家自营 > 图书报刊/音像」并过审（原「文娱 > 出版物」为内容服务类目、与实物电商不匹配，保留不删），但购物订单仍提示"经营类目超出范围"；当日仅能确认类目自助路径不可用，不能据此证明其阻断 JSAPI 支付。另：隐私指引因剪贴板用途说明不符被驳回，后于 2026-08-21 由用户确认已解决；wx-trade 模块代码（2026-08-13 完成）已提交 `1a84062` 并推送，云托管自动部署 `server-031`（流量 100%），线上 diag/storefront 实测正常、`/api/wx-trade/status` 已上线（需管理员鉴权）；`WX_TRADE_MODE` 线上保持缺省 disabled，购物订单/发货管理开通状态待单独验收
- 2026-08-13：当时按已有资料完成微信订单发货管理（发货信息管理服务）初版——新增 `wx-trade` 模块、发货同步状态、管理后台手动重试、运力编码表、开通诊断与订单详情 path，server build、56 个单元测试、43 个 E2E、admin build 通过；部署后保持 `WX_TRADE_MODE=disabled`。2026-08-21 按微信当前官方 API 逐字段复核后发现订单键类型与上传时间格式不符，故该能力重新标记为待修复；旧版“购物订单”与当前订单发货管理继续分开判断
- 2026-08-12：买家订单详情接口兼容商户订单号——`GET /api/buyer/orders/:id` 同时接受内部 UUID 与 `orderNo`（`O-YYYYMMDD-XXXX`），为微信订单中心跳转（path 携带 `${商品订单号}`）铺路；openid 归属校验不变。server build、42 个单元测试、38 个 E2E 通过；已提交 `4a43435` 并推送，云托管自动部署 `server-030`（流量 100%），线上 diag/storefront 实测正常。另：当时真机出现 `requestPayment` 付款能力限制，曾推断根因是未接入「购物订单」并发送开通申请邮件；2026-08-21 两笔真实支付及后台落库成功后，该因果归因因证据不足已撤回，历史失败原因因缺少原始 `errMsg/errCode/errno` 和同时间服务日志仍无法确认。生产库商品价格已临时改为 0.10 元供验收，验收后须改回 168.00 元
- 2026-08-12：用户确认微信后台配置完成——`request` 合法域名已加入 `https://xn--4oqx7jk6ghq7b.com`，《用户隐私保护指引》已按实际收集信息（地址/手机号/剪贴板）完善，UGC 声明为不包含；订单中心 path 暂不设，待买家订单列表页与微信订单发货管理一并接入。下一步：真机全流程验收（登录 → 下单 → 真实支付 → 回调落库 → 地址 → 结果页）与线上管理员换密
- 2026-08-12：云托管新代码部署完成，线上从 `wx-login-v3` 切换到 `wx-login-v4`。根因：`eb0cdcd` 新增的生产启动校验（`production-readiness.ts`）要求 `WX_LOGIN_MODE=real`/`WX_PAY_MODE=real`，而云托管环境变量缺这两项 → 新镜像容器启动即崩（server-026 部署失败）→ 之后两次「配置变更」版本（server-025/027）均复用 7 月 23 日 server-024 旧镜像，线上一直停留在 v3。修复：控制台补入两个 mode 变量后从 `feat/ui-redesign` 重新构建部署。实测 `/api/wx/diag` 返回 `wx-login-v4`、`loginMode=real`，`/api/products/storefront` 返回默认直营入口。本次起本机已全局安装 CloudBase 插件/MCP 与 `@cloudbase/cli`（3.7.3）并完成 `tcb login` 授权，后续可用 `tcb cloudrun detail/record/logs` 直接排查线上问题。剩余待办：微信后台配置（合法域名、隐私指引）、线上管理员换密、真机全流程验收
- 2026-08-07：Phase 5 Task 4 代码与自动验证完成——新增 `GET /api/products/storefront`，仅接受显式 `DEFAULT_SELLER_CODE`/`DEFAULT_PRODUCT_ID`，缺失或不可用时返回 503；小程序无 scene 时加载直营商品，有 scene 时保持销售归因。隐私同意前允许浏览但不登录、不记录扫码、不购买；剪贴板仅在主动点击并同意后读取，拒绝可手填。地址页补信息用途、隐私与客服入口，结果页补订单/地址/物流/退款客服并将入群改为自愿；首发 `app.json` 移除销售方/老板端页面入口，启用隐私与域名检查。server build、39 个单元测试、37 个 E2E、miniprogram build、JSON 解析和差异检查通过；本地 `.env` 已按用户确认配置直营销售方与默认商品，云托管新版部署、微信后台与真机验收待办
- 2026-08-07：Phase 5 Task 3 代码与自动验证完成——`POST /api/wx/login` 改为返回短期 buyer JWT，小程序只缓存 token；新增 `/api/buyer/orders` 列表/详情/创建/地址/支付/对账接口并逐单校验 openid，原 `/api/orders` 仅允许管理员；扫码日志也从买家认证上下文取 openid。操作日志不记录登录及买家请求体，管理请求敏感字段递归脱敏；生产不再自动播种固定管理员，改为显式一次性初始化；生产 CORS 默认拒绝跨域。server build、39 个单元测试、35 个 E2E、miniprogram build 全部通过；线上管理员换密、真机双账号和数据库日志人工验收待办
- 2026-08-06：Phase 5 Task 2 代码与自动验证完成——新增显式 `WX_LOGIN_MODE`/`WX_PAY_MODE`，生产环境启动时校验真实登录/支付配置，development/test 才允许显式 mock；配置异常返回 503 且不修改订单，移除微信登录 `rejectUnauthorized: false`，诊断版本升为 `wx-login-v4`。server build、34 个单元测试、32 个 E2E 全部通过；未修改真实 `.env`，云托管配置、关闭自签代理、部署和真实登录验收待用户确认
- 2026-08-06：用户确认域名 ICP 备案已完成、小程序备案进入最后一步；将当前阶段更新为 Phase 5，完成上线差距梳理并新增 `docs/superpowers/plans/2026-08-06-phase5-miniprogram-launch-readiness.md`。同步区分“历史 0.01 元支付冒烟”和“当前发布候选完整验收”，移除已过时的 `/api/wx/login` 500 阻塞，补入真实小程序码、微信订单发货管理、生产 fail-closed、订单鉴权、隐私、退款客服和分销合规等 P0 待办
- 2026-07-31：排查公安备案「网站打不开」问题——根因是域名 `优学启航.com` 无任何 DNS 解析记录。已申请免费 SSL 证书（DNS 手动验证）、云托管绑定主域 + www 自定义域名并上传证书、DNSPod 配置 CNAME，外网验证两地址 HTTPS 均 200（管理后台登录页），可重新接受公安核查
- 2026-07-23：家长端地址页增加一键粘贴识别（剪贴板/文本框 + 本地解析省市区姓名手机），降低手工填写成本
- 2026-07-23：真机支付 0.01 成功后填地址无跳转——`pages/buyer/result/result` 未注册进 `app.json`，已补登记并给 navigateTo fail 提示
- 2026-07-23：login 已到可读错误：`self-signed certificate`（云托管「开放接口服务」代理 api.weixin.qq.com 自签证书）。代码对微信域名 `rejectUnauthorized:false`（diag 升为 wx-login-v3）；官方亦建议自行 jscode2session 时关闭开放接口服务后重新发布
- 2026-07-23：联调点购买仍 toast「Internal server error」；探测线上 login 仍为无 timestamp 的裸 500（说明上一版加固可能未生效或异常未进 HttpExceptionFilter）。再加固：增加 `GET /api/wx/diag` 版本探针、login 全路径捕获、小程序 api 控制台打印完整错误
- 2026-07-23：联调中产品页可加载；点购买失败因 `/api/wx/login` 500。加固 `wx-login.service`（Node https、超时、网络/业务错误分别 503/400 并回传原因），便于区分 Secret 错误与云托管出网问题
- 2026-07-23：用户确认云托管 `WX_*` 环境变量已配置；探测线上 `POST /api/wx/*` 曾为 404，已 push 支付模块后路由恢复（notify 401 缺签名头为预期）；回调入口可达，登录换 openid 仍待修
- 2026-07-23：Phase 3.2 微信支付真实集成代码完成——新增 `payments` 模块（JSAPI 下单、回调验签解密、主动对账、wx.login 换 openid）、订单 mock/真实双模式、小程序端 `wx.requestPayment` 接入；修复 `wechatpay-axios-plugin` 运行时导入错误（`WechatpayAxiosPlugin` 命名空间仅存在于类型声明）；单测 24 个、E2E 30 个全部通过
- 2026-07-21：确认小程序信息/类目/微信认证已完成；澄清 Phase 3.2 阻塞项——小程序备案可与开发并行，ICP 备案对云托管默认域名不强依赖，唯一硬前置是微信支付商户号（待用户申请）
- 2026-07-21：同步用户侧资质进展：出版物经营许可证已取得，域名 ICP 备案进行中，微信小程序备案待办，抖店资质审核与铺货进行中
- 2026-07-09：修复管理后台新增销售方 parentId 空字符串校验、二维码解析 UUID 格式兼容、小程序产品详情 API 路径、二维码图片 URL 列长度不足导致生成失败的问题
- 2026-07-07：修复管理后台登录提示"用户名或密码错误"的问题，后端启动时自动创建固定默认管理员账号（生产环境必须移除固定口令播种）
- 2026-07-07：管理后台集成到后端服务完成，后端容器统一托管管理后台静态文件，API 地址改为相对路径 `/api`，本地 API 和前端路由验证通过
- 2026-07-06：Phase 4 部署准备完成，后端成功部署到微信云托管 dev 环境，数据库迁移自动运行，13 张业务表已创建
- 2026-07-06：Phase 4 部署准备开始，创建 GitHub 仓库 https://github.com/Imsage323/book-sales-referral，添加后端 Dockerfile、.dockerignore 与 TypeORM 迁移脚本
- 2026-07-06：Phase 3.3 完成，实现售后观察期结算、返点规则/记录管理、一层直接推荐奖励，使用占位返点规则，单元测试与 E2E 测试全部通过
- 2026-07-06：Phase 3.4（订单/发货台账 + 销售汇总）完成，新增 `LedgerModule`、Excel 导出接口、管理后台台账导出页面，单元测试与 E2E 测试全部通过
- 2026-07-06：Phase 3.2（占位版）完成，实现模拟支付接口、小程序支付确认页、`payment_events` 记录，单元测试与 E2E 测试全部通过
- 2026-07-06：Phase 3.1 完成，实现扫码产品页、订单创建、地址填写、发货管理及对应管理后台和小程序页面
- 2026-07-06：Phase 2 完成，销售方、产品、二维码模块及对应管理后台页面开发完成，单元测试与 E2E 测试全部通过
- 2026-07-06：Phase 1 完成，数据库迁移和 E2E 测试通过

## 维护规则

- 每次完成开发、修复、文档补齐或重要调研后，必须同步更新本文件。
- 纯查询、只读分析、临时命令、未改变项目状态的操作，不需要更新本文件。
- 只有已经实现并验证过的事项才能放进“已完成”。
- 未确认的信息写“待确认”，不要猜。
- 做完代码但未验证时，不得把事项标为已完成。
- 本文件只写会变化的进度，不写固定不变的项目背景。
